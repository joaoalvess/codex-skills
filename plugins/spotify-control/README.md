# Spotify Control

Control Spotify playback, volume, playback modes, and current-track lookup from Codex on macOS.

## Skill included

- `spotify-control`

## Notes

- This plugin wraps the existing `spotify-control` skill as its own installable plugin.
- It expects the Spotify macOS app to be installed locally.
