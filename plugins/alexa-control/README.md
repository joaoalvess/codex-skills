# Alexa Control

Control Amazon Alexa devices, routines, smart-home actions, and playback from Codex on macOS.

## Skill included

- `alexa-control`

## Notes

- This plugin wraps the existing `alexa-control` skill as its own installable plugin.
- It expects a local `alexa-remote-control` checkout and an authenticated Alexa session.
