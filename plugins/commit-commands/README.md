# Commit Commands

Automate Git and GitHub publish workflows from Codex with grouped conventional commits and optional PR creation.

## Skill included

- `commit-commands`

## Notes

- This plugin wraps the existing `commit-commands` skill as its own installable plugin.
- It expects a Git repository with an `origin` remote, and `gh` authenticated for the `commit-push-pr` flow.
